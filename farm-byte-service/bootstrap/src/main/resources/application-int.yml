spring:
  jpa:
    open-in-view: false
    show-sql: true
    generate-ddl: true
  flyway:
    locations: classpath:db/migrations/common
logging:
  level:
    org.springframework.web: DEBUG
server:
  port: ${APP_PORT}