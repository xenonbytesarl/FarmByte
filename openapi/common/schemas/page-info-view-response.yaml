PageInfoViewResponse:
  type: object
  required:
    - first
    - last
    - pageSize
    - totalElement
    - totalPages
  properties:
    first:
      type: boolean
    last:
      type: boolean
    pageSize:
      type: integer
      format: int32
    totalElements:
      type: integer
      format: int64
    totalPages:
      type: integer
      format: int32